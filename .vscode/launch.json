{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "ShoppingList.sln",
            "type": "coreclr",
            "request": "attach",
            "processId": "${command:azureFunctions.pickProcess}"
        }
    ]
}